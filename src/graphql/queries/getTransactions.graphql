query getTransactions($where:transaction_bool_exp) {
  transaction(where:$where) {
    id
    createdAt
    updatedAt
    description
    amount
    status
    category
    transactionDate
    postDate
    accountId
  }
}